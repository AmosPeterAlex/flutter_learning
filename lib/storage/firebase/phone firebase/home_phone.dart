import 'package:flutter/material.dart';

class HomePhoneVerif extends StatelessWidget {
  const HomePhoneVerif({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Home'),
      ),
    );
  }
}
